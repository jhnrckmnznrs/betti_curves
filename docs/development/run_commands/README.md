# Historical optimization run commands

These command sheets reproduce experimental checkpoints that preceded the v23.6 U16 H2 production freeze. They are retained for benchmark provenance and should not be treated as current production instructions.

Use the repository-root `V23_6_RUN_COMMANDS.txt` for the current production-freeze validation and smoke run.
